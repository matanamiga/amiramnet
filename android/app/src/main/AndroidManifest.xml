<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permission to drive the phantom command running inside Termux. -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />
    <!-- Camera: scan the gate's pairing QR. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Android 11+ package visibility: we need to see / talk to Termux. -->
    <queries>
        <package android:name="com.termux" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Phantom">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:label="@string/scan_qr" />

        <!-- Receives stdout/stderr back from the Termux RUN_COMMAND service. -->
        <receiver
            android:name=".PhantomResultReceiver"
            android:exported="true" />
    </application>
</manifest>
