# Minimal deps to run the test suite in CI (no GUI automation libs needed —
# screen/pyautogui are lazily imported and stubbed in tests).
pydantic>=2.5
requests>=2.31
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
qrcode>=7.0
pytest>=8.0
zeroconf>=0.131
